import { Search } from "lucide-react";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import type { TradeResult } from "../types";
export function TradeHistory({ trades }: { trades: TradeResult[] }) { return <main className="min-h-0 flex-1 overflow-auto bg-workspace p-4 lg:p-6"><div className="mx-auto max-w-7xl"><div className="mb-5"><div className="label">Transaction ledger</div><h1 className="mt-2 text-2xl font-semibold">Trade history</h1><p className="mt-1 text-sm text-muted-foreground">Prototype records for interface evaluation only.</p></div><div className="mb-3 grid gap-2 sm:grid-cols-4"><div className="relative"><Search className="absolute left-2.5 top-2.5 size-4 text-muted-foreground"/><Input placeholder="Search contract ID" className="pl-8"/></div>{["All markets","All contracts","All results"].map((x,i)=><Select key={x} defaultValue="all"><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="all">{x}</SelectItem>{i===2&&<><SelectItem value="won">Won</SelectItem><SelectItem value="lost">Lost</SelectItem></>}</SelectContent></Select>)}</div><div className="overflow-x-auto border border-border bg-panel"><table className="w-full min-w-[880px] text-left text-xs"><thead className="border-b border-border bg-surface text-[9px] uppercase tracking-[0.12em] text-muted-foreground"><tr>{["Time","Market","Contract","Direction / prediction","Stake","Payout","Profit / loss","Result","Contract ID"].map(h=><th key={h} className="px-3 py-3 font-semibold">{h}</th>)}</tr></thead><tbody>{trades.map(t=><tr key={t.id} className="border-b border-border/70 hover:bg-accent"><td className="px-3 py-3 font-mono">{t.time}</td><td className="px-3 py-3">{t.market}</td><td className="px-3 py-3">{t.contract}</td><td className="px-3 py-3">{t.direction}</td><td className="px-3 py-3 font-mono">${t.stake.toFixed(2)}</td><td className="px-3 py-3 font-mono">${t.payout.toFixed(2)}</td><td className={`px-3 py-3 font-mono ${t.profit>=0?"text-positive":"text-negative"}`}>{t.profit>=0?"+":""}${t.profit.toFixed(2)}</td><td className="px-3 py-3"><span className={t.result==="Won"?"text-positive":"text-negative"}>{t.result}</span></td><td className="px-3 py-3 font-mono text-muted-foreground">{t.id}</td></tr>)}</tbody></table></div></div></main> }
