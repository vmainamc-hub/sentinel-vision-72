import { Activity, BrainCircuit, ChevronDown, ChevronUp, CircleGauge, RadioTower } from "lucide-react";
import type { SentinelState } from "../types";
export function SentinelIntelligence({ state, open, onToggle }: { state: SentinelState; open: boolean; onToggle: () => void }) { const items=[{l:"Psychology",v:`${state.psychology}%`,i:BrainCircuit},{l:"Pressure",v:`${state.pressure}%`,i:CircleGauge},{l:"Momentum",v:`${state.momentum>0?"+":""}${state.momentum}`,i:Activity},{l:"Parity",v:state.parity,i:RadioTower},{l:"Losing threat",v:state.threat},{l:"Liquidity",v:state.liquidity},{l:"Feed",v:state.feed},{l:"Market",v:state.market}]; return <section className="border-t border-border bg-surface"><button onClick={onToggle} className="flex w-full items-center justify-between px-3 py-2.5"><div className="flex items-center gap-3"><div className="grid size-7 place-items-center border border-primary/30 bg-primary/10 text-primary"><BrainCircuit className="size-4"/></div><div className="text-left"><div className="label">Sentinel live intelligence</div><div className="text-[10px] text-positive">ANALYSIS READY · MANUAL EXECUTION</div></div></div>{open?<ChevronUp className="size-4"/>:<ChevronDown className="size-4"/>}</button>{open&&<div className="grid grid-cols-2 gap-px border-t border-border bg-border sm:grid-cols-4 lg:grid-cols-8">{items.map(({l,v,i:Icon})=><div key={l} className="bg-panel p-3">{Icon&&<Icon className="mb-2 size-3 text-primary"/>}<div className="text-[9px] uppercase text-muted-foreground">{l}</div><div className={`mt-1 font-mono text-xs font-semibold ${["LIVE","OPEN","CLEAR","LOW"].includes(String(v))?"text-positive":""}`}>{v}</div></div>)}</div>}</section> }
